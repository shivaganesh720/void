# Workflow and Evaluation Report

The only executable workflow is the bounded Resume/JD path. There is no general workflow builder, versioned workflow, conditional graph, activation state, or workflow API.

There is no evaluation service or dashboard. Existing tests cover parsing, matching, structured output, evidence, and invalid input as regression checks, but datasets, expected/actual run records, groundedness, safety, latency, cost, and historical comparison are not implemented.
