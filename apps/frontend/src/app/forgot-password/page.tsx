"use client";
import Link from "next/link";
import { FormEvent, useState } from "react";
export default function ForgotPasswordPage() { const [sent, setSent] = useState(false); function submit(event: FormEvent) { event.preventDefault(); setSent(true); } return <main className="auth-page"><section className="auth-visual"><Link className="brand-lockup" href="/"><span className="brand-orbit">◌</span><span><b>VOID</b><small>Controlled execution</small></span></Link><div><p className="eyebrow">ACCOUNT RECOVERY</p><h1>Find your way<br /><em>back to work.</em></h1><p>We&apos;ll help you safely recover access to your workspace.</p></div></section><section className="auth-panel"><div className="auth-form-wrap"><p className="eyebrow">FORGOT PASSWORD</p><h2>Reset your password</h2><p className="muted">Enter your email and we&apos;ll send recovery instructions when email delivery is configured.</p>{sent ? <><p className="success-message">If an account exists for that address, recovery instructions will be sent.</p><Link className="button button-primary button-wide" href="/login">Return to sign in</Link></> : <form onSubmit={submit}><label>Email address<input type="email" required autoComplete="email" /></label><button className="button button-primary button-wide">Send recovery link</button></form>}<p className="auth-switch"><Link href="/login">Back to sign in</Link></p></div></section></main>; }
